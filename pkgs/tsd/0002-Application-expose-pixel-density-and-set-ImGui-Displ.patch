Commit ID: 0000000000000000000000000000000000000000
Change ID: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
Author   : Thomas Arcila <134677+tarcila@users.noreply.github.com> (2026-04-20 10:22:45)
Committer: Thomas Arcila <134677+tarcila@users.noreply.github.com> (2026-04-20 10:22:45)

    Application: expose pixel density and set ImGui DisplayFramebufferScale

diff --git a/tsd/src/tsd/ui/imgui/Application.cpp b/tsd/src/tsd/ui/imgui/Application.cpp
index 1234567890..abcdef1234 100644
--- a/tsd/src/tsd/ui/imgui/Application.cpp
+++ b/tsd/src/tsd/ui/imgui/Application.cpp
@@ -30,6 +30,7 @@ struct Application::AppImpl
 {
   SDL_Window *sdlWindow{nullptr};
   SDL_Renderer *sdlRenderer{nullptr};
+  float pixelDensity{1.f};
   int width{0};
   int height{0};
   bool windowResized{true};
@@ -69,6 +70,11 @@ SDL_Window *Application::sdlWindow()
   return m_impl->sdlWindow;
 }
 
+float Application::pixelDensity() const
+{
+  return m_impl->pixelDensity;
+}
+
 void Application::run(int width, int height, const char *name)
 {
   m_impl->width = width;
@@ -1131,8 +1137,8 @@ void Application::AppImpl::init(Uint32 windowFlags)
 
   SDL_ShowWindow(sdlWindow);
 
-  const float scale = SDL_GetWindowPixelDensity(sdlWindow);
-  SDL_SetRenderScale(sdlRenderer, scale, scale);
+  pixelDensity = SDL_GetWindowPixelDensity(sdlWindow);
+  SDL_SetRenderScale(sdlRenderer, pixelDensity, pixelDensity);
 
   ImGui::CreateContext();
   ImGui::StyleColorsDark();
@@ -1143,6 +1149,7 @@ void Application::AppImpl::init(Uint32 windowFlags)
   ImGuiIO &io = ImGui::GetIO();
   io.ConfigFlags |= ImGuiConfigFlags_NavEnableKeyboard;
   io.ConfigFlags |= ImGuiConfigFlags_DockingEnable;
+  io.DisplayFramebufferScale = ImVec2(pixelDensity, pixelDensity);
 
   ImGuiStyle &style = ImGui::GetStyle();
   if (io.ConfigFlags & ImGuiConfigFlags_ViewportsEnable)
diff --git a/tsd/src/tsd/ui/imgui/Application.h b/tsd/src/tsd/ui/imgui/Application.h
index 2345678901..bcdef12345 100644
--- a/tsd/src/tsd/ui/imgui/Application.h
+++ b/tsd/src/tsd/ui/imgui/Application.h
@@ -51,6 +51,7 @@ class Application
 
   SDL_Renderer *sdlRenderer();
   SDL_Window *sdlWindow();
+  float pixelDensity() const;
 
   // Start the application run loop
   void run(int width, int height, const char *name);
